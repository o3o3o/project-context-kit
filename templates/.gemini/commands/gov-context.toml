prompt = """
Restore the current working context for a new session.
1. Read `.ai-governance/docs/project/metadata.yaml` to understand the repository structure and commands.
2. Use the `activate_skill` tool with `name='task-context'` to synthesize the current GCC state.
"""
description = "Restore current GCC context (Gemini CLI)"
