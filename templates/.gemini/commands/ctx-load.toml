prompt = """
Restore the current working context for a new session.
1. Read `.project-context/docs/project/metadata.yaml` to understand the repository structure and commands.
2. Read `.project-context/docs/task/active/index.md` for the current active task status.
3. Use the `activate_skill` tool with `name='task-context'` to synthesize the current project context state.
"""
description = "Restore current project context (Gemini CLI)"
